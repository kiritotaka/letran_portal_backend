# Copy to .env. Never commit real secrets.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Reserved for a later phase. Optional and unused in phase 1.
GEMINI_API_KEY=
# Comma-separated origins; no paths or trailing slash. Empty disables CORS.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

